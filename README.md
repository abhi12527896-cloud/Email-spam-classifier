# Email-spam-classifier
A production-ready machine learning pipeline for spam detection. Train a Multinomial Naive Bayes classifier on labeled text data, evaluate performance with detailed metrics, and deploy it for real-time classification—all from the command line.
